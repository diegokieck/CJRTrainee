<%= bootstrap_form_for([@article, @article.comments.build]) do |f| %>
  <p>
    <%= f.text_field :commenter %>
  </p>
  <p>
   
    <%= f.text_area :body %>
  </p>
  <p>
    <%= f.submit %>
  </p>
<% end %>